// -------- VALID BLOCK 1 --------main() {    int a;    float b;    double x;    bool flag;    a = 10 + 5;    b = a * 2.5;    x = b + 3.14;    flag = true;}// -------- VALID BLOCK 2 (while loop) --------main() {    int i;    i = 0;    while (i < 10) {        i = i + 1;    }}// -------- VALID BLOCK 3 (mixed types in loop) --------main() {    int a;    float f;    bool cond;    a = 5;    f = 2.5;    cond = true;    while (cond) {        a = a + 1;        cond = false;    }}// -------- INVALID BLOCK 1 (undeclared and missing names) --------main() {    x;                     // ? 'x' undeclared    float ;                // ? Missing name    bool ;                 // ? Missing name}// -------- INVALID BLOCK 2 (undeclared in loop & type mismatch) --------main() {    int count;    while (flag) {         // ? 'flag' undeclared        count = count + 1;    }    count = true;          // ? int = bool}// -------- INVALID BLOCK 3 (redeclaration & invalid assignment) --------main() {    int a;    int a;                 // ? Redeclaration    float b;    b = "hello";           // ? float = string}